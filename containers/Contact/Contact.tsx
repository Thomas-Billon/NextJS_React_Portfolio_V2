import React from 'react';
import {  } from '@/Config';

import './Contact.scss';


const Contact = (): React.ReactNode => {
    return(
        <section id="contact">
        </section>
    );
};

export default Contact;