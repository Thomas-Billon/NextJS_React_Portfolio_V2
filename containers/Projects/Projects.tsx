import React from 'react';
import {  } from '@/Config';

import './Projects.scss';


const Projects = (): React.ReactNode => {
    return(
        <section id="projects">
        </section>
    );
};

export default Projects;