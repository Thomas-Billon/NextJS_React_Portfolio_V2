import React from 'react';
import {  } from '@/Config';

import './Skills.scss';


const Skills = (): React.ReactNode => {
    return(
        <section id="skills">
        </section>
    );
};

export default Skills;